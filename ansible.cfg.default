[defaults]
log_path=./drupsible.log

[ssh_connection]
# Enable this to authenticate against external services (like Github).
# Remember to SSH the controller also with ForwardAgent=yes.
ssh_args = -o ForwardAgent=yes -o ControlMaster=auto -o ControlPersist=30m
pipelining=True
